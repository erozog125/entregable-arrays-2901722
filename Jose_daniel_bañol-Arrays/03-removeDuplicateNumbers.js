const numbers = [2, 2, 3, 4, 10, 6, 7, 3, 9, 10];

function removeDuplicateNumbers() {
    let deleteNumbers = new Set(numbers)

    let newArray = [...deleteNumbers]

    return newArray

} 

  

  
