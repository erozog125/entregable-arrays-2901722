function reverseArray(array) {
    return array.slice().reverse();
  }
  
  // Usage example
  const originalArray = [3, 8, 2, 5, 1];
  console.log(reverseArray(originalArray)); // This will return [1, 5, 2, 8, 3]