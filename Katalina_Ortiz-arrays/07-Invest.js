// Definir la función que devuelve un nuevo array con los elementos en orden inverso
function invertArray(array) {
    // Utilizamos el método reverse para invertir el orden del array
    const arrayInverted = array.reverse();
    
    // Devolvemos el array invertido
    return arrayInverted;
}
